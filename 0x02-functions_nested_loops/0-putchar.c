#include "main.h"
/**
 * main - starting of the program
 *
 * Return: (0) if succesfull
 */

int main(void)
{
int count;
char arr[9];

	for (count = 0; count <= 9; ++count)
	{
		_putchar(arr[count]);
		_putchar('/n');
	}

	return (0);
}
